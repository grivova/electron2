// В этом файле будет храниться конфигурация фронтенда.
// Перед сборкой для реального использования (`npm run build`),
// нужно заменить 'http://localhost:3001' на реальный IP-адрес
// и порт машины, где будет запущен backend.
// Например: 'http://192.168.1.10:3001'

const config = {
    backendUrl: 'http://localhost:3001'
};

export default config; 